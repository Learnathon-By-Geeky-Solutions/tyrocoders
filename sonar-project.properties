# Project Information (Replace with your project details)
sonar.projectKey=Learnathon-By-Geeky-Solutions_tyrocoders
sonar.projectName=tyrocoders
sonar.organization=learnathon-by-geeky-solutions
sonar.host.url=https://sonarcloud.io 

# Analyze only the 'backend/' folder (adjust path if needed)
sonar.sources=backend/

# Include test files (if applicable)
sonar.test.inclusions=backend/**/test_*.py, backend/**/tests/**

# Exclusions (Next.js frontend + FastAPI boilerplate)
sonar.exclusions=
  frontend/**,
  **/.next/**,
  **/node_modules/**,
  **/public/**,
  **/*.css,
  **/*.scss,
  **/*.js,
  **/*.jsx,
  **/*.ts,
  **/*.tsx,
  **/*.svg,
  **/*.json,
  **/next.config.js,
  **/next-env.d.ts,
  **/__pycache__/**,
  **/migrations/**,
  **/alembic/**,
  **/static/**,
  **/templates/**,
  **/venv/**,
  **/*.html